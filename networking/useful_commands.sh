# For more DNS-related commands, see folder 'dns' under project root.

dig

gping <address>

host <address>

netstat -p tcp

nslookup <address>

ping <address>

traceroute <address>

whois <address>

ip a
